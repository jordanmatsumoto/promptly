from django.apps import AppConfig

class PromptlyConfig(AppConfig):
    """Configuration settings for the Promptly Django app."""
    
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'promptly'
